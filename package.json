{
  "name": "milo-dsl",
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "bun": ">=1.1"
  },
  "scripts": {
    "build": "bun run build.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
