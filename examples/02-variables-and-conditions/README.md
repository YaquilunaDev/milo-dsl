# 02 - variables and conditions

Declares a prompt-captured variable and an init-declared variable, asks
the user for input, branches on their answer, and loops. Shows
`html.evalVar()` for inline substitution and `js`...`` for the condition.

## Run

```bash
bun run examples/02-variables-and-conditions/build.ts
```
